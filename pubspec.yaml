name: iehrplus
description: "A Flutter WebView application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.0.0
  webview_flutter_wkwebview: ^3.16.3
  flutter_inappwebview: ^6.1.5
  url_launcher: ^6.3.1
  connectivity_plus: ^6.1.0
  permission_handler: ^11.3.1
  share_plus: ^10.1.2

dev_dependencies:
  icons_launcher: ^3.0.0
  rename: ^3.0.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
